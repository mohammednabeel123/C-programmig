#include <stdio.h>

int main() {
    int a = 34, b = 44, c = 5;
    int result = a + b + c;

    printf("The sum of %d, %d, %d\n", a, b, c);  // \n adds a newline
    printf("is: %d\n", result);                 // \n moves to next line

    return 0;
}


