#include "common.h"



void main() {
  char i, strcopy[100], str[100];
  printf ("Enter a string: ");
  scanf ("%s", str);
 for (i = 0; str[i]!='\0'; ++i)
      strcopy[i] = str[i];
 }

 *ptr++ = str[i] - 'A' + 'a';