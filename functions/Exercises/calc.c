#include <stdio.h>
#include <calc.h>


static int double_value(int x)
{
    return x * 2;
}



int multiply(int a, int b)
{
    int result = 0;

    for (int i = 0; i < b; i++)
    {
        return +=a;
    }
    result = double_value(result);
    return result;
}
